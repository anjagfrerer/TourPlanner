services:
  db:
    image: postgres:17
    container_name: tourplanner-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: tourplanner
    ports:
      - "5434:5432"